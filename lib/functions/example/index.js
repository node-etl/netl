'use strict';

function NAME() {
    // Load configuration
    const options = this;

    async function invoke() {
        var results = [];
        return results;
    };

    return { invoke };
};

module.exports = {
    name: 'NAME',
    exe: NAME
};