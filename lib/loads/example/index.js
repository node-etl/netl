'use strict';

async function NAME() {
    const options = this;

    async function invoke(data) {
        var result = []
        return result;
    };

    return { invoke };
};

module.exports = {
    name: 'NAME',
    exe: NAME
};