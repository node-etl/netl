'use strict';

async function NAME() {
    const options = this;

    async function invoke(obj) {
        return [];
    };

    return { invoke };
};

module.exports = {
    name: 'NAME',
    exe: NAME
};